project_id = "bbopyxeqlymtndtomiwx"

# Reject unauthenticated probes at the gateway so the isolate does not boot
# on every random scan. Cron jobs send the anon apikey/JWT so they keep working.
[functions.auto-submit-missed-leave]
verify_jwt = true

[functions.auto-checkout]
verify_jwt = true

[functions.auto-weekly-task-credit]
verify_jwt = true

[functions.task-deadline-sweep]
verify_jwt = true

[functions.monthly-reset]
verify_jwt = true
