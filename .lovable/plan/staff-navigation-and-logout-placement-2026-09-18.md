# Staff navigation and logout placement

## Changes
- Make the Staff mobile navigation use the same left-side slide-out menu pattern as Admin and Assistant Admin.
- Keep each role’s existing menu items, order, labels, visibility rules, and destinations unchanged.
- Remove the separate Logout control from the bottom-right navigation bar for every role.
- Keep Logout available inside the left-side menu, with the existing confirmation dialog.

## Scope and validation
- Change presentation code only; do not alter permissions, routes, data, attendance, leave, tasks, salary, or backend behavior.
- Verify the updated navigation on mobile-sized Staff and Admin views, then confirm the project builds successfully.
